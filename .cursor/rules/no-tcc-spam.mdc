---
description: 禁止触发 macOS 权限弹窗（屏幕录制/辅助功能 TCC）
alwaysApply: true
---

# 永不可破：不要再弹权限窗

本项目跑在 macOS「脚本合集」里，**反复弹「屏幕录制 / 辅助功能」会卡死流程**。用户已明确要求永久记住。

## 硬禁止

1. **禁止**用 `screencapture`（默认 `ALLOW_SCREEN_CAPTURE = False`；禁止探针、启动自检）。
2. **禁止**每次启动对已有二进制 `codesign --force`；重签会换 CDHash，系统当成新程序再弹窗。
3. **禁止**无意义 `touch` 源码触发重编（尤其 `scripts/*.swift`）。
4. **禁止**在回放/点击热路径里截图。
5. **禁止**权限自检 UI / `permission_report` 探测。
6. **禁止**日常「重启界面」走 `force_rebuild_tools` / `rebuild: true`。
7. **禁止**手校记点路径调用 `capture_window` / `screencapture`。

## 允许

- 手校点位 + 绝对 CG 回放点击（不依赖截屏）。
- 用户已勾选权限时，如需临时开识图：把 `ALLOW_SCREEN_CAPTURE` 改为 `True`（会再弹录屏，慎用）。
- 界面「重启界面」：只重启 GUI 进程。

## 改代码前自检

- 会不会调用 screencapture / 开 ALLOW_SCREEN_CAPTURE？
- 会不会重编 click/ocr/clickwatch/hotkey/wininfo/overlay？
- 重启会不会默认 rebuild？

有一条是「会」→ 必须改掉再提交思路。
