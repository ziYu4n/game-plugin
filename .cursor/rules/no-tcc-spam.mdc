---
description: 禁止触发 macOS 权限弹窗（屏幕录制/辅助功能 TCC）
alwaysApply: true
---

# 永不可破：不要再弹权限窗

本项目跑在 macOS「脚本合集」里，**反复弹「屏幕录制 / 辅助功能」会卡死流程**。用户已明确要求永久记住。

## 硬禁止

1. **禁止**用 `screencapture` 做权限探针（含 `perm_probe`、启动自检、`permission_report(probe_screen=True)`）。
2. **禁止**每次启动对已有二进制 `codesign --force`；**只在真正重新编译后**签一次。重签会换 CDHash，系统当成新程序，权限作废再弹窗。
3. **禁止**无意义 `touch` 源码触发重编（尤其 `scripts/*.swift`）。
4. **禁止**在回放/点击热路径里频繁截图；截图失败必须进入冷却，禁止连环重试叠弹窗。
5. 改点击/OCR/录制逻辑时，默认假设：**权限已授过一次，绝不能主动再触发授权 UI**。

## 允许

- 业务必需时才 `capture_window`（OCR/识图），且复用新鲜截图、失败冷却。
- 用户已勾选「屏幕录制 + 辅助功能」给「脚本合集」（或实际启动进程）；缺权限时日志提示勾选，不要探针验证。

## 改代码前自检

- 这次改动会不会重编 click/ocr/clickwatch/hotkey？
- 会不会新增启动时的 screencapture？
- 失败路径会不会每秒截一次图？

有一条是「会」→ 必须改掉再提交思路。
